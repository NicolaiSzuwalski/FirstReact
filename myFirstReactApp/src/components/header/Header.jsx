import styles from './Header.module.scss'

export const Header = () => {
    return(
        <header className={styles.headerWrapper}>
            <h1>This is a react app</h1>
            </header>
    )
}